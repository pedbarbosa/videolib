json_file:
    'videolib.json'

scan_path:
    '<video folder location>'
    
html_report:
    '<where to store the html report>'

codec_override:
    ['this_one', 'that_one']

ignore_folders:
    ['.', '..', '.DS_Store']

video_extensions:
    ['.avi', '.m4v', '.mkv', '.mp4', '.mpg', '.ts']

# Optional entries
# recode_report:
#     '<where to store the recode report>'
# recode_cp_target:
#     '<where to copy files that require recoding>'
# recode_disk:
#     '<mount point for recode disk>'
